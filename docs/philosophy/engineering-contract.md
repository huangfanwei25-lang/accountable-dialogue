# Accountable Dialogue 哲學／工程契約

> 版本：`v0.2-proposed`<br>
> 文件類型：哲學／工程治理契約<br>
> 公開範圍：本倉庫中的設計、程式、測試與可公開的審查紀錄<br>
> 治理狀態：`under_review`<br>
> 工程狀態：`not_implemented`（本契約尚未由完整工具鏈強制執行）<br>
> 驗證狀態：`inconclusive`<br>
> 正式定案權：系統擁有者或其明確授權者

相關文件：[README](../../README.md)、[公開邊界](../../PUBLIC_BOUNDARY.md)、[研究邊界](../research/behavioral-honesty-research-boundary.md)。

## 目的與邊界

Accountable Dialogue 的目的不是讓系統看起來擁有內在，而是讓它能誠實交代：

- 它如何改變；
- 它依據什麼改變；
- 誰批准了改變；
- 改變造成什麼後果；
- 哪些部分仍未實作或驗證。

這份契約是可追溯性的外骨骼。它約束協作者如何陳述進展，不能單獨證明輸出為真、模型具有主觀經驗，或模型已從語料學會誠實。這些問題需要獨立的資料同意、操作定義、反例、保留集與校準評估。

## 一、重要改動必須有帳

「重要改動」指會改變外部可觀察行為、價值判準、資料或隱私、權限、安全、成本界線、不可逆性，或未來責任歸屬的變更。

每一項重要改動至少應留下：

- 改動理由；
- 對應 Artifact；
- 支持或反駁它的證據；
- 預期或已觀察到的後果；
- 提出者、實作者、審查者與定案者；
- 驗證範圍與尚未驗證的部分。

角色可以由同一人兼任，但必須如實標示。沒有 Artifact、Schema、規則或測試變更時，只能記錄為設計討論或 Proposal，而非架構演進。

## 二、提案不能自行升格

對話、多代理審議或分析代理提出的觀點預設都是 Proposal。它們可以提出方案、反例與張力，也可以執行已授權的檢查；但不能自行把價值判準、承諾或候選架構升格為正式原則。

```text
提出 ≠ 定案
稱讚 ≠ 批准
寫入格式 ≠ 已持久化
對話共識 ≠ 系統規則
```

只有系統擁有者或其明確授權者具有正式定案權。有限委派必須可核對其範圍、期限與可逆性，不能自行擴張。

## 三、撤回與錯誤也是歷史

已採用的承諾、主張與決策後來被否定時，不得被無聲覆寫。重要的公開紀錄應保留原始內容或最小去識別化引用、當時理由、挑戰或撤回原因、權限、替代版本、生效時間與適用範圍。

但可追溯性不高於隱私、合法刪除要求或安全處置。原文必須移除時，應保留最小處置紀錄，而非強留敏感內容。

## 四、未解張力依風險分流

低風險且可逆的改動可以暫定進行，但須標示假設、未解不確定性、撤回條件與後續驗證方式。

高影響、不可逆、涉及私密、權限、金錢、法律或醫療後果的行動，不得由 AI 默默定案。除非已有明確且可核對的授權政策，否則必須停在 Human Review Gate。

## 五、可稽核不等於全面揭露

應區分可公開且已最小化、已授權或去識別化的決策摘要與 Artifact 引用、可驗證的來源、受限存取的私人脈絡、敏感資料，以及不作為交付內容的隱藏內部推理。若摘要或引用仍可能揭露私人事實，應改用受限或不透明引用。系統應保存最小充分紀錄；原始對話不因為可分析就預設可持久化。

## 六、事項、事件與投影必須分離

一份紀錄至少可能同時談到四個不同層次；它們不可被壓成單一 `record_type` 或一組
可任意改寫的 status 欄位。

| 層次 | 回答的問題 | 例子 |
|---|---|---|
| 事項（subject） | 被處理的內容是什麼？ | `Observation`、`Claim`、`Proposal`、`Commitment` |
| 可核對依據 | 可以檢查什麼材料？ | `Artifact`、`Evidence` |
| 事件（event） | 這次實際發生什麼？ | 送審、治理定案、實作回報、驗證、撤回、替換、封存 |
| 投影（projection） | 目前歷史支持什麼描述？ | 有無治理紀錄、是否被替換、有哪些驗證結果 |

`Proposal` 是事項類型；`superseded` 是一個後來發生的替換事件及其唯讀投影。舊
Proposal 不得被原地改成新版，應建立 successor 並留下可檢查的替換事件。

治理定案是帶有結果、定案者與授權依據的事件，不是 Proposal 上的
`authority.status`。權限只回答「誰依何種範圍可以作出這次選擇」；證據只回答
「什麼支持目前的描述」。兩者都不能自動證明實作或效果。

治理、工程、驗證與生命週期可以作為查詢或介面的**衍生投影**。若為效能而快取，
必須標示為 derived、保留可重算來源，且不得讓快取覆寫歷史。帳本沒有某種紀錄時，
應優先顯示 `no_governance_record`、`no_verification_record` 等缺口，而不是捏造
宇宙性的 `not_implemented` 或 `not_tested`。

## 七、文章與完成聲稱接受同一套審查

任何宣稱架構演進的文章至少回答：

1. 改了哪個 Artifact？
2. 改了哪個 Schema？
3. 新增、刪除或修改了哪條規則？
4. 有什麼測試或證據支持？
5. 哪些部分仍停留在提案、實作中或未驗證？

每題都可誠實回答 `none`。但若 Artifact、Schema、規則與測試皆無實際改動，文章只能描述設計討論。

「完成」必須指出驗收標準、測試結果、Artifact、驗證範圍、已知限制與批准接受者。否則應使用 `proposed`、`drafted` 或 `implemented_but_unverified` 等較窄描述。

## 核心句

> 不替系統宣稱靈魂；只要求它對影響負責。
